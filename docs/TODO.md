# TODO

## Deferred

- Optional native-text PDF fast path: OCR is now the primary PDF extraction path; a future fast path may use native PDF text when it is reliable.
- Job URL crawling: improve dynamic-page crawling, anti-bot handling, login-required pages, and site-specific extraction.
- Job matching agent: replace the scaffold evaluator after job crawling produces cleaner content.
- Resume optimization agent: add section-level STAR/STAT suggestions with evidence after parsing and LaTeX generation are stable.
